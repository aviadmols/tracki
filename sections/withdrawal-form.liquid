<div class="withdrwal-info-wrap">
{% for block in section.blocks %}
  {% case block.type %}
    {% when '@app' %}
      {% render block %}
  {% endcase %}
{% endfor %}
</div>

{% schema %}
{
  "name": "Withdrawal Form",
  "settings": [
    ],
  "blocks": [
  {
    "type": "@app"
  }
],
  "presets": [
    {
      "name": "Withdrawal Form"
    }
  ]
}
{% endschema %}