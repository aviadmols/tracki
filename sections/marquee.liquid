<div class="announcement-marquee" style="background: {{ section.settings.bg_color }}; color: {{ section.settings.text_color }};">
  <div class="announcement-track">
    {% for i in (1..2) %}
      {% for block in section.blocks %}
        <span class="announcement-item">
          {% if block.settings.link != blank %}
            <a href="{{ block.settings.link }}">{{ block.settings.text }}</a>
          {% else %}
            {{ block.settings.text }}
          {% endif %}
          <span class="separator">•</span>
        </span>
      {% endfor %}
    {% endfor %}
  </div>
</div>

<style>
.announcement-marquee {
  overflow: hidden;
  white-space: nowrap;
  padding: 12px 0;
}

.announcement-track {
  display: inline-flex;
  align-items: center;
  animation: marquee {{ section.settings.speed }}s linear infinite;
}

.announcement-item {
  display: inline-flex;
  align-items: center;
  font-size: 16px;
  font-weight: 700;
  text-transform: uppercase;
}

.announcement-item a {
  color: inherit;
  text-decoration: none;
}

.separator {
  margin: 0 30px;
}

@keyframes marquee {
  from {
    transform: translateX(0);
  }
  to {
    transform: translateX(-50%);
  }
}
</style>

{% schema %}
{
  "name": "Announcement Marquee",
  "settings": [
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#111111"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "speed",
      "label": "Scroll Speed",
      "min": 10,
      "max": 60,
      "step": 5,
      "unit": "s",
      "default": 25
    }
  ],
  "blocks": [
    {
      "type": "announcement",
      "name": "Announcement",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "FREE SHIPPING ON ORDERS OVER $200"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Announcement Marquee",
      "blocks": [
        {
          "type": "announcement"
        },
        {
          "type": "announcement"
        }
      ]
    }
  ]
}
{% endschema %}